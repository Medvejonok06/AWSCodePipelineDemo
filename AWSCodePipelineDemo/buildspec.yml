version: 0.2
phases:
  install:
    runtime-versions:
      dotnet: 10.0
  build:
    commands:
      - dotnet publish -c Release -o ./app AWSCodePipelineDemo/AWSCodePipelineDemo.csproj
artifacts:
  files:
    - '**/*'
  base-directory: app
